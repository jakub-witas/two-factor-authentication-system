{
  "scripts": {
    "test": "vitest",
    "test:db:up": "docker-compose -f docker-compose.test.yml up -d",
    "test:db:down": "docker-compose -f docker-compose.test.yml down -v",
    "testfull": "npm run test:db:up && sleep 5 && vitest && npm run test:db:down",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@faker-js/faker": "^9.2.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "express-validator": "^7.2.1",
    "fs": "^0.0.1-security",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.5",
    "otplib": "^12.0.1",
    "pg": "^8.13.1",
    "redis": "^4.7.1",
    "sequelize": "^6.37.5",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4",
    "xss-clean": "^0.1.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4"
  }
}
